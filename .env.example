# Copy to .env and fill in values. .env is git-ignored.
# PostgreSQL connection used by the ML pipeline / db scripts:
PHARMA_DB_URL=postgresql://postgres:1828@localhost:5432/medcare
# LLM API key for digest generation (optional, falls back to template):
GROQ_API_KEY=
